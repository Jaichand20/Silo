# Ingestion Service

Handles Steps 1-3 of the plan: PDF extraction/chunking, embedding via
Ollama, and vector storage in ChromaDB. Independent FastAPI service
(Step 6), containerized separately (Step 3).
