# UI Service

A static HTML/CSS/JS single-page interface for uploading documents and
chatting with them, served by Python's built-in `http.server`. Runs entirely
in the browser and talks to the ingestion and chat services only over REST,
same as any other client would.
